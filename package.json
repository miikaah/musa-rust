{
  "name": "musa-rust",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "author": "Miika Henttonen <miika.henttonen@wunderdog.fi>",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "electron": "IS_DEV=true electron .",
    "build": "electron-build-env neon build --release",
    "restart": "electron-build-env neon build --release && npm run electron"
  },
  "devDependencies": {
    "electron": "^4.0.3",
    "electron-build-env": "^0.2.0",
    "neon-cli": "^0.2.0"
  }
}
